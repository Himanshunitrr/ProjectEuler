#include <bits/stdc++.h>
 
 
using namespace std;
int main()
{
    int num, sum = 0;
    cin >> num;
    for(int i = 1;i < num;i++)
    {
        if(i % 3 == 0 || i % 5 == 0)
        {
            sum = i + sum;
        }
    }
    cout << sum;
    return 0;
}
